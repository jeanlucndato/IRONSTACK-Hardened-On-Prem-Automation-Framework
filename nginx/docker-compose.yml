version: '3.8'

services:
  nginx_ingress:
    image: nginx:latest
    container_name: nginx_ingress  # TRÈS IMPORTANT : doit correspondre au config.yaml
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: always

